name: dbt
version: '1.0'
profile: spark_local

config-version: 2

seeds:
  dbt_project:
    file_format: csv 
    header: true

models:
  dbt_project: 
    staging:
      materialized: view
    marts:
      materialized: table